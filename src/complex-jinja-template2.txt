{% extends 'admin/model/edit.html' %}
{% block head_css %}
  {{ super() }}
  <style>
    /* 드롭다운이 열려있을 때의 z-index 조정 */
    .select2-container--open {
      z-index: 9999;
    }
    /* 스크롤 중에도 드롭다운이 보이도록 */
    .select2-dropdown-above-scroll {
      z-index: 10000;
    }
    /* 드롭다운 최대 높이 설정 */
    .select2-results__options {
      max-height: 200px;
      overflow-y: auto;
    }
  </style>
{% endblock %}
{% block edit_form %}
  {% if current_user.permission.name in ["developer", "admin"] %}
    <div class="admin-form form-horizontal">
      <h4 style="line-height: 40px;">
        <button class="btn btn-success btn-xs pull-right" onclick="addPayment()">+입금추가</button>
        최종금액: {{ "{:,}".format(model.final_cost) }} <br/>
        <table class="table table-bordered" style="max-width: 600px; margin-bottom: 0px;">
          {% for payment in model.payments %}
            <tr>
              <td>{{ "{:,}".format(payment.amount) }}원</td>
              <td>{{ payment.created_at.strftime('%Y-%m-%d %H:%M') }}</td>
              <td>{{ payment.memo }}</td>
              <td>
                <button class="btn btn-danger btn-xs" onclick="deletePayment({{ payment.id }})">삭제</button>
              </td>
            </tr>
          {% endfor %}
        </table>
        미수금액: {{ "{:,}".format(model.final_cost - paid) }} <br/>
      </h4>
    </div>
  {% endif %}
  {{ super() }}
{% endblock %}
{% block tail_js %}
  {{ super() }}
  <script>
    window.id = {{ request.args.get('id') }};
  </script>
  <script src="{{ url_for('static', filename='js/admin/request/edit.js', v=app_start_time) }}"></script>
{% endblock %}